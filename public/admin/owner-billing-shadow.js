const $=(id)=>document.getElementById(id);
const esc=(v)=>String(v??"").replace(/[&<>"']/g,(c)=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const money=(v)=>`${Number(v||0).toLocaleString("fr-FR")} Ar`;
const label=(v)=>({commercial:"Commercial",trial:"En test",internal:"Interne",exempt:"Exempté",commission:"Commission",subscription:"Abonnement",issued:"Émise",calculated:"Calculé",recorded:"Enregistré"})[v]||v||"—";
async function api(url){const r=await fetch(url,{credentials:"include"}),t=await r.text();let d={};try{d=JSON.parse(t)}catch{d={error:"non_json"}}if(!r.ok)throw new Error(d.error||"request_failed");return d}
function empty(id,text){$(id).innerHTML=`<div class="ob-empty">${esc(text)}</div>`}
function render(d){const byPool=Object.fromEntries((d.assignments||[]).map(a=>[a.pool_id,a]));$("pools").innerHTML=(d.pools||[]).length?(d.pools||[]).map(p=>{const a=byPool[p.id];return `<article class="ob-card"><h2>${esc([p.brand_name,p.name].filter(Boolean).join(" – ")||p.name)}</h2><div class="ob-muted">${esc(p.radius_nas_id||"")}</div>${a?`<div class="ob-pills"><span class="ob-pill ob-ok">${esc(label(a.billing_status))}</span><span class="ob-pill">${esc(label(a.billing_mode))}</span></div><div class="ob-muted">Effet : ${esc(a.effective_from)}</div>`:`<div class="ob-pills"><span class="ob-pill">Aucune offre active</span></div>`}</article>`}).join(""):"<div class=\"ob-empty\">Aucun pool propriétaire associé à cette session.</div>";
$("invoices").innerHTML=(d.invoices||[]).length?(d.invoices||[]).map(i=>`<article class="ob-doc"><strong>${esc(i.invoice_number)}</strong>${esc(i.offer_title_snapshot)} · ${money(i.amount_due_ar)}<br>Période ${esc(i.period_start)} → ${esc(i.period_end)} · ${esc(label(i.status))}<br><span class="ob-muted">Échéance : ${esc(String(i.due_at||"").slice(0,10))} · PDF indisponible en Shadow</span></article>`).join(""):(empty("invoices","Aucune facture d’abonnement Shadow."),$("invoices").innerHTML);
const payoutByStatement=Object.fromEntries((d.payout_records||[]).map(x=>[x.commission_statement_id,x]));$("statements").innerHTML=(d.statements||[]).length?(d.statements||[]).map(s=>{const p=payoutByStatement[s.id];return `<article class="ob-doc"><strong>Relevé ${esc(s.period_start)} — ${esc(s.offer_title_snapshot)}</strong>Ventes : ${money(s.gross_sales_ar)} · Commission ${Number(s.commission_pct)} % : ${money(s.commission_amount_ar)}<br>Montant propriétaire avant frais : ${money(s.owner_gross_amount_ar)} · ${Number(s.transaction_count)} vente(s)<br><span class="ob-muted">${p?`Reversement test : ${money(p.net_owner_amount_ar)} (non exécuté)`:"Aucun reversement enregistré"}</span></article>`}).join(""):(empty("statements","Aucun relevé de commission Shadow."),$("statements").innerHTML);
$("documents").innerHTML=(d.documents||[]).length?(d.documents||[]).map(x=>`<article class="ob-doc"><strong>${esc(x.title)}</strong>${esc(label(x.status))} · ${money(x.amount_ar)}<br><span class="ob-muted">Document Shadow · téléchargement PDF indisponible</span></article>`).join(""):(empty("documents","Aucun document Shadow."),$("documents").innerHTML)}
async function load(){try{const me=await api("/api/admin/me");$("me").textContent=`Connecté : ${me.email||"propriétaire"}`;render(await api("/api/owner/billing-shadow"))}catch(e){$("error").style.display="block";$("error").textContent=e.message}}
$("refreshBtn").onclick=load;load();
